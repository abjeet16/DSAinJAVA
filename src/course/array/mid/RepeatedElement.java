package course.array.mid;

/**
 * so here the array has only one repeated element and
 * the array will have all element from 0 to max value element
 */
public class RepeatedElement {
    public static void main(String[] args) {
        int[] arr = {1,3,2,4,3,3};
    }
}
